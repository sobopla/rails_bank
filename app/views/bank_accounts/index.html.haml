%h1
  Bank Accounts

%table.table.table-bordered
  %thead
    %tr
      %th Account number
      %th Balance
      %th Actions
  %tbody
    - @bank_account.each do |bank_account|
      %tr
        %td #{bank_account.account_number}
        %td #{number_to_currency(bank_account.balance)}
        %td
          = link_to bank_account_path(bank_account.id), class: "btn btn-primary" do
            Show

